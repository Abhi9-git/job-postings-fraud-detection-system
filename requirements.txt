# Runtime dependencies for Vercel deployment (app.py imports only).
# Versions pinned to match the trained .pkl artifacts — do not bump
# scikit-learn/numpy/pandas/scipy without retraining.
Flask==3.1.3
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.9.0
scipy==1.18.1
requests==2.34.2
beautifulsoup4==4.15.0
